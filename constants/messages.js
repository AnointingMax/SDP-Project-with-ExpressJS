const noPermission = "You do not have permission to do that";

const wrongCredentials = "Wrong Login Credentials Provided";

const noAuth = "You are not authenticated";

const deleteSuccess = "Deleted succesfully";

const createdSuccess = "Created succesfully";

const updatedSuccess = "Edited succesfully";

module.exports = {
  noPermission,
  wrongCredentials,
  noAuth,
  deleteSuccess,
  createdSuccess,
  updatedSuccess,
};
